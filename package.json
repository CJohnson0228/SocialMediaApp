{
  "name": "socialmediaapp",
  "version": "1.0.0",
  "description": "a full stack social media app using them MERN stack with typescript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client",
    "server": "npm run app --prefix server"
  },
  "author": "C.M. Johnson",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
